node_modules
dist
.env
.env.local

# Large raw video assets — not part of the deployed site (hosted on Supabase instead)
videos/
videos-for-supabase-upload/
*.zip

# Local Netlify folder
.netlify
deno.lock
*.log

# Raw logo/accreditation source files — one has an unresolved stock
# watermark + spelling errors, not ready to ship yet (see conversation)
logos/
